# Database Directory

This folder will contain database configuration and migrations:

- `schema.sql` - Database table definitions
- `seeds.sql` - Sample data for testing
- `config.js` - Database connection configuration

We'll use PostgreSQL for the database.
